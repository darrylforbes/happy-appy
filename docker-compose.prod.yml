version: "3.7"

services:
  api:
    image: docker.pkg.github.com/happy-appy/happy-appy-api
    env_file:
      - ./.env.prod

  frontend:
    image: docker.pkg.github.com/happy-appy/happy-appy-frontend

  nginx:
    image: docker.pkg.github.com/happy-appy/happy-appy-nginx
    networks:
      - default
      - nginx-network
    depends_on:
      - api
      - frontend

networks:
  nginx-network:
    external:
      name: nginx-network
